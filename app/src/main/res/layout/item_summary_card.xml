<?xml version="1.0" encoding="utf-8"?>
<!--
    item_summary_card.xml

    Root is a FrameLayout with clipChildren=false so the glow
    can bleed outside the card boundaries.

    Layer order (bottom → top):
      1. GlowView  — radial gradient glow, sits below and behind the card
      2. CardView  — the actual card content
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="112dp"
    android:layout_height="100dp"
    android:layout_marginEnd="6dp"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!--
        GlowView — wider and taller than the card so the gradient
        bleeds outward on all sides. Starts invisible (alpha=0).
        Alpha and color are set from Java at runtime.
    -->
    <com.example.notifly_system.GlowView
        android:id="@+id/viewGlow"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginStart="-10dp"
        android:layout_marginEnd="-10dp"
        android:layout_marginTop="18dp"
        android:layout_marginBottom="-12dp"
        android:alpha="0" />

    <!-- CardView — sits on top of the glow -->
    <androidx.cardview.widget.CardView
        android:id="@+id/summaryCard"
        android:layout_width="100dp"
        android:layout_height="80dp"
        android:layout_gravity="top|center_horizontal"
        app:cardCornerRadius="12dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="#1E3A4A">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="8dp">

            <TextView
                android:id="@+id/tvCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="22sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#AACCDD"
                android:textSize="11sp"
                android:gravity="center"
                android:layout_marginTop="2dp" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</FrameLayout>
